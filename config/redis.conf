# Persistence
appendonly yes
appendfsync everysec

# Data directory (must match the volume mount in docker-compose.yml)
dir /data

# Keep an RDB snapshot as a backup alongside AOF
save 3600 1
save 300 100
save 60 10000
